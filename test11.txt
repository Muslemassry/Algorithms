function QuestionsMarks(str) { 
  var regularOne = /[0-9]\?{3}[0-9]/;
  var regularTwo = /[^0-9]/;
  
  // code goes here  
  index=0;
  count=1;
  while (true) {
      index = str.search(count,regularOne);
      if (index == -1) {
        break;  
      }
      
      if ((str.charAt(index) + str.charAt(index+4)) == 10) {
          return "true";
      } else {
          count++;
      }
  }
  
  index=0;
  count=1;
  while (true) {
      index = str.search(count,regularTwo);
      if (index == -1) {
        break;  
      }
      
      index2 = 0;
      count2=1;
      while(true) {
          index2 = str.search(count2,regularTwo);
          if (index2 == -1) {
              break;
          } else if (index2 == index) {
              count2++;
          } else if ((str.charAt(index) + str.charAt(index2)) == 10){
              return "true";
          }   
      }
  }
  
  
  return "false";
         
}
   
// keep this function call here 
QuestionsMarks(readline()); 
